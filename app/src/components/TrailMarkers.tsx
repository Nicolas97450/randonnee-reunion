// TrailMarkers — MapLibre temporarily disabled
// Markers will render on the real map once MapLibre is re-enabled

interface Props {
  onTrailPress?: (slug: string) => void;
}

export default function TrailMarkers(_props: Props) {
  return null;
}
